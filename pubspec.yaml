name: icon_loading_button
description: Icon loading button implementation of Rounded loading button
version: 1.0.0
homepage: https://github.com/AsimBhadra/icon_loading_button

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.27.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
